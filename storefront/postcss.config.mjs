/**
 * Tailwind CSS v4 is configured CSS-first: the design tokens live in
 * `src/styles/globals.css` under `@theme`, so there is no tailwind.config.ts.
 */
const config = {
  plugins: {
    '@tailwindcss/postcss': {},
  },
};

export default config;
